/**
 * SectionPanel moved to the shared UI layer (components/ui/SectionPanel).
 * Re-exported here so existing dashboard imports keep working.
 */
export { SectionPanel } from "@/components/ui/SectionPanel";
export type { SectionPanelProps } from "@/components/ui/SectionPanel";
