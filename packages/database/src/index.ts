// Generated Prisma types, enums (e.g. RoleName, UserStatus) and the
// PrismaClient class. Re-exported so the app depends on a single entry point.
export * from "@prisma/client";

// The shared singleton instance — prefer this over `new PrismaClient()`.
export { prisma } from "./client";
